apiVersion: v1
kind: Service
metadata:
  name: stocks-analyst-service
  labels:
    app: stocks-analyst
spec:
  type: NodePort
  ports:
  - port: 8501
    targetPort: 8501
    nodePort: 30501
  selector:
    app: stocks-analyst
